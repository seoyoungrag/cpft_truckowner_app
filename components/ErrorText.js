import React from "react";
import {View, Text} from "react-native";

export default (props) => {
	return (
		<View style={{flex: 1, alignItems: "center", marginTop: "50%"}}>
			<Text style={{fontSize: 20}}>에러 발생</Text>
		</View>
	);
};
